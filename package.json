{
  "name": "mural",
  "version": "1.0.0",
  "description": "Muro público de mensajes (guestbook) full-stack sin bundler — Fastify + Prisma + PostgreSQL (Neon)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "@prisma/client": "^6.0.0",
    "fastify": "^5.0.0",
    "prisma": "^6.0.0"
  }
}
